#include "main.h"
#include <stdio.h>

/**
 * main - prints name of the program
 * @argc: number of arguments
 * @argv: array name
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
